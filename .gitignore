node_modules
.env
dist
dev.db
dev.db-journal

/generated/prisma

# Claude Code worktrees
.claude/worktrees/

# Backups locais (dados sensiveis - nunca commitar)
backups/

# Segredos e dados legados — nunca versionar
vapid.env
vapid*.env
keys.txt
result_keys.txt
data/users.json
data/tokens.json
data/user_history.json
